'use strict';

/**
 * @ngdoc function
 * @name webApp.controller:UserLoginCtrl
 * @description
 * # UserLoginCtrl
 * Controller of the script2Bit
 */
script2Bit.controller('UserLoginCtrl', ['$scope', function ($scope) {

  $scope.init = function() {
  };

  $scope.init();

  }]);
